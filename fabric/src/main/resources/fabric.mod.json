{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",

  "name": "${mod_name}",
  "description": "${description}",
  "authors": ["${authors}"],
  "contact": {},

  "license": "${license}",
  "icon": "assets/${mod_id}/mod_icon.png",

  "environment": "*",
  "entrypoints": {
    "client": ["com.example.mc.examplemod.fabric.client.ExampleModFabricClient"],
    "main": ["com.example.mc.examplemod.fabric.ExampleModFabric"]
  },

  "mixins": [
    "${mod_id}.fabric.mixins.json",
    "${mod_id}.mixins.json"
  ],

  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": "*",
    "minecraft": "${minecraft_version}"
  }
}
